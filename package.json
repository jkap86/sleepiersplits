{
  "name": "sleepier-splits",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && pip install -r requirements.txt && gunicorn app:app",
    "postinstall": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC"
}